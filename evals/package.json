{
  "name": "kosh-skill-evals",
  "private": true,
  "engines": {
    "node": "^20.20.0 || >=22.22.0"
  },
  "scripts": {
    "reset": "node scripts/reset-workspace.mjs",
    "eval": "npm run reset && promptfoo eval --no-cache"
  },
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.211",
    "promptfoo": "^0.121.19"
  }
}
