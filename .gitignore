# Dependencies — tests are a manual check (see tests/README.md); `npm install jsdom`
# creates these locally and they are deliberately not committed
node_modules/
package.json
package-lock.json

# Test output — reports and raw data are client-specific, not committed
reports/data/
reports/screenshots/
reports/*.html
reports/*.md

# Local Claude settings
.claude/settings.local.json

# Playwright MCP logs
.playwright-mcp/

# OS
.DS_Store